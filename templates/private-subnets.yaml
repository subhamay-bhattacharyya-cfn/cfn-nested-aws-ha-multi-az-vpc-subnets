# Enhanced NESTED STACK 2: Private Subnets with NACLs and Multi-AZ NAT Gateways
# File: private-subnets-enhanced.yaml
# Purpose: Create private subnets with optional multi-AZ NAT Gateways and NACLs
 
AWSTemplateFormatVersion: '2010-09-09'
Description: 'Enhanced nested template for private subnets with NACLs and multi-AZ NAT Gateways'
 
Parameters:
  ApplicationPrefix:
    Type: String
    Description: Application name prefix (used for resource naming)
    AllowedPattern: '^[a-zA-Z][-a-zA-Z0-9]*$'
    ConstraintDescription: Must start with a letter and contain only alphanumeric characters and hyphens

  EnvironmentName:
    Type: String
    Description: Environment name prefix

  VPCId:
    Type: AWS::EC2::VPC::Id
    Description: VPC ID to attach subnets
  
  SubnetCount:
    Type: Number
    Description: Number of private subnets to create
    MinValue: 1
    MaxValue: 4
  
  SubnetCidrBlocks:
    Type: CommaDelimitedList
    Description: CIDR blocks for private subnets
  
  AvailabilityZones:
    Type: CommaDelimitedList
    Description: Availability Zones for subnets (DEPRECATED - use PrivateSubnetAZ1-4 for individual control)

  PrivateSubnetAZ1:
    Type: AWS::EC2::AvailabilityZone::Name
    Description: Availability Zone for Private Subnet 1

  PrivateSubnetAZ2:
    Type: AWS::EC2::AvailabilityZone::Name
    Description: Availability Zone for Private Subnet 2

  PrivateSubnetAZ3:
    Type: AWS::EC2::AvailabilityZone::Name
    Description: Availability Zone for Private Subnet 3

  PrivateSubnetAZ4:
    Type: AWS::EC2::AvailabilityZone::Name
    Description: Availability Zone for Private Subnet 4

  EnableNATGateway:
    Type: String
    Description: Enable NAT Gateway for internet egress
    AllowedValues:
      - 'true'
      - 'false'
  
  EnableHighAvailabilityNAT:
    Type: String
    Description: Create one NAT Gateway per AZ (requires NAT enabled)
    AllowedValues:
      - 'true'
      - 'false'
  
  PublicSubnetIds:
    Type: CommaDelimitedList
    Description: Public Subnet IDs (for NAT Gateway placement)
 
  EnableNACLs:
    Type: String
    Description: Enable Network ACLs for private subnets
    Default: 'true'
    AllowedValues:
      - 'true'
      - 'false'

  PrivateSubnetNamePrefix:
    Type: String
    Description: Name prefix for private subnets (leave empty to use ApplicationPrefix-pvt-subnet)
    Default: ''

  PrivateRouteTableNamePrefix:
    Type: String
    Description: Name prefix for private route tables (leave empty to use ApplicationPrefix-pvt-rt)
    Default: ''

Conditions:
  UseAppPrefixForPrivateSubnets: !Equals [!Ref PrivateSubnetNamePrefix, '']
  UseAppPrefixForPrivateRT: !Equals [!Ref PrivateRouteTableNamePrefix, '']
  CreatePrivateSubnet1: !Or
    - !Equals [!Ref SubnetCount, 1]
    - !Equals [!Ref SubnetCount, 2]
    - !Equals [!Ref SubnetCount, 3]
    - !Equals [!Ref SubnetCount, 4]
 
  CreatePrivateSubnet2: !Or
    - !Equals [!Ref SubnetCount, 2]
    - !Equals [!Ref SubnetCount, 3]
    - !Equals [!Ref SubnetCount, 4]
 
  CreatePrivateSubnet3: !Or
    - !Equals [!Ref SubnetCount, 3]
    - !Equals [!Ref SubnetCount, 4]
 
  CreatePrivateSubnet4: !Equals [!Ref SubnetCount, 4]
 
  EnableNAT: !Equals [!Ref EnableNATGateway, 'true']
  EnableHANAT: !And
    - !Condition EnableNAT
    - !Equals [!Ref EnableHighAvailabilityNAT, 'true']
  
  CreateNATGateway1: !And
    - !Condition EnableNAT
    - !Condition CreatePrivateSubnet1
  
  CreateNATGateway2: !And
    - !Condition EnableHANAT
    - !Condition CreatePrivateSubnet2
  
  CreateNATGateway3: !And
    - !Condition EnableHANAT
    - !Condition CreatePrivateSubnet3
  
  CreateNATGateway4: !And
    - !Condition EnableHANAT
    - !Condition CreatePrivateSubnet4
 
  CreateNACLs: !Equals [!Ref EnableNACLs, 'true']
 
Resources:
  # ============================================================================
  # Private Subnets
  # ============================================================================
 
  PrivateSubnet1:
    Type: AWS::EC2::Subnet
    Condition: CreatePrivateSubnet1
    Properties:
      VpcId: !Ref VPCId
      CidrBlock: !Select [0, !Ref SubnetCidrBlocks]
      AvailabilityZone: !Ref PrivateSubnetAZ1
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateSubnets, !Sub '${ApplicationPrefix}-pvt-subnet-1', !Sub '${PrivateSubnetNamePrefix}-1']
        - Key: Type
          Value: Private
 
  PrivateSubnet2:
    Type: AWS::EC2::Subnet
    Condition: CreatePrivateSubnet2
    Properties:
      VpcId: !Ref VPCId
      CidrBlock: !Select [1, !Ref SubnetCidrBlocks]
      AvailabilityZone: !Ref PrivateSubnetAZ2
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateSubnets, !Sub '${ApplicationPrefix}-pvt-subnet-2', !Sub '${PrivateSubnetNamePrefix}-2']
        - Key: Type
          Value: Private
 
  PrivateSubnet3:
    Type: AWS::EC2::Subnet
    Condition: CreatePrivateSubnet3
    Properties:
      VpcId: !Ref VPCId
      CidrBlock: !Select [2, !Ref SubnetCidrBlocks]
      AvailabilityZone: !Ref PrivateSubnetAZ3
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateSubnets, !Sub '${ApplicationPrefix}-pvt-subnet-3', !Sub '${PrivateSubnetNamePrefix}-3']
        - Key: Type
          Value: Private
 
  PrivateSubnet4:
    Type: AWS::EC2::Subnet
    Condition: CreatePrivateSubnet4
    Properties:
      VpcId: !Ref VPCId
      CidrBlock: !Select [3, !Ref SubnetCidrBlocks]
      AvailabilityZone: !Ref PrivateSubnetAZ4
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateSubnets, !Sub '${ApplicationPrefix}-pvt-subnet-4', !Sub '${PrivateSubnetNamePrefix}-4']
        - Key: Type
          Value: Private
 
  # ============================================================================
  # Private Route Tables (one per AZ for granular routing)
  # ============================================================================
 
  PrivateRouteTable1:
    Type: AWS::EC2::RouteTable
    Condition: CreatePrivateSubnet1
    Properties:
      VpcId: !Ref VPCId
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateRT, !Sub '${ApplicationPrefix}-pvt-rt-1', !Sub '${PrivateRouteTableNamePrefix}-1']
        - Key: Type
          Value: Private
 
  PrivateRouteTable2:
    Type: AWS::EC2::RouteTable
    Condition: CreatePrivateSubnet2
    Properties:
      VpcId: !Ref VPCId
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateRT, !Sub '${ApplicationPrefix}-pvt-rt-2', !Sub '${PrivateRouteTableNamePrefix}-2']
        - Key: Type
          Value: Private
 
  PrivateRouteTable3:
    Type: AWS::EC2::RouteTable
    Condition: CreatePrivateSubnet3
    Properties:
      VpcId: !Ref VPCId
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateRT, !Sub '${ApplicationPrefix}-pvt-rt-3', !Sub '${PrivateRouteTableNamePrefix}-3']
        - Key: Type
          Value: Private
 
  PrivateRouteTable4:
    Type: AWS::EC2::RouteTable
    Condition: CreatePrivateSubnet4
    Properties:
      VpcId: !Ref VPCId
      Tags:
        - Key: Name
          Value: !If [UseAppPrefixForPrivateRT, !Sub '${ApplicationPrefix}-pvt-rt-4', !Sub '${PrivateRouteTableNamePrefix}-4']
        - Key: Type
          Value: Private
 
  PrivateSubnet1RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreatePrivateSubnet1
    Properties:
      SubnetId: !Ref PrivateSubnet1
      RouteTableId: !Ref PrivateRouteTable1
 
  PrivateSubnet2RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreatePrivateSubnet2
    Properties:
      SubnetId: !Ref PrivateSubnet2
      RouteTableId: !Ref PrivateRouteTable2
 
  PrivateSubnet3RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreatePrivateSubnet3
    Properties:
      SubnetId: !Ref PrivateSubnet3
      RouteTableId: !Ref PrivateRouteTable3
 
  PrivateSubnet4RouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Condition: CreatePrivateSubnet4
    Properties:
      SubnetId: !Ref PrivateSubnet4
      RouteTableId: !Ref PrivateRouteTable4
 
  # ============================================================================
  # Multi-AZ NAT Gateways with Elastic IPs
  # ============================================================================
 
  NATGatewayEIP1:
    Type: AWS::EC2::EIP
    Condition: CreateNATGateway1
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-eip-1'
 
  NATGateway1:
    Type: AWS::EC2::NatGateway
    Condition: CreateNATGateway1
    Properties:
      AllocationId: !GetAtt NATGatewayEIP1.AllocationId
      SubnetId: !Select [0, !Ref PublicSubnetIds]
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-gw-1'
 
  PrivateRoute1:
    Type: AWS::EC2::Route
    Condition: CreateNATGateway1
    Properties:
      RouteTableId: !Ref PrivateRouteTable1
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NATGateway1
 
  # NAT Gateway 2 (AZ 2) - Only if HA NAT enabled and subnet exists
  NATGatewayEIP2:
    Type: AWS::EC2::EIP
    Condition: CreateNATGateway2
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-eip-2'
 
  NATGateway2:
    Type: AWS::EC2::NatGateway
    Condition: CreateNATGateway2
    Properties:
      AllocationId: !GetAtt NATGatewayEIP2.AllocationId
      SubnetId: !Select [1, !Ref PublicSubnetIds]
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-gw-2'
 
  PrivateRoute2:
    Type: AWS::EC2::Route
    Condition: CreateNATGateway2
    Properties:
      RouteTableId: !Ref PrivateRouteTable2
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NATGateway2
 
  # NAT Gateway 3 (AZ 3) - Only if HA NAT enabled and subnet exists
  NATGatewayEIP3:
    Type: AWS::EC2::EIP
    Condition: CreateNATGateway3
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-eip-3'
 
  NATGateway3:
    Type: AWS::EC2::NatGateway
    Condition: CreateNATGateway3
    Properties:
      AllocationId: !GetAtt NATGatewayEIP3.AllocationId
      SubnetId: !Select [2, !Ref PublicSubnetIds]
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-gw-3'
 
  PrivateRoute3:
    Type: AWS::EC2::Route
    Condition: CreateNATGateway3
    Properties:
      RouteTableId: !Ref PrivateRouteTable3
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NATGateway3
 
  # NAT Gateway 4 (AZ 4) - Only if HA NAT enabled and subnet exists
  NATGatewayEIP4:
    Type: AWS::EC2::EIP
    Condition: CreateNATGateway4
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-eip-4'
 
  NATGateway4:
    Type: AWS::EC2::NatGateway
    Condition: CreateNATGateway4
    Properties:
      AllocationId: !GetAtt NATGatewayEIP4.AllocationId
      SubnetId: !Select [3, !Ref PublicSubnetIds]
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-nat-gw-4'
 
  PrivateRoute4:
    Type: AWS::EC2::Route
    Condition: CreateNATGateway4
    Properties:
      RouteTableId: !Ref PrivateRouteTable4
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NATGateway4
 
  # ============================================================================
  # Private Network ACLs
  # ============================================================================
 
  PrivateNetworkAcl:
    Type: AWS::EC2::NetworkAcl
    Condition: CreateNACLs
    Properties:
      VpcId: !Ref VPCId
      Tags:
        - Key: Name
          Value: !Sub '${EnvironmentName}-private-nacl'
        - Key: Type
          Value: Private
 
  # Inbound Rules - Private NACL
  PrivateNACLInboundFromPublic:
    Type: AWS::EC2::NetworkAclEntry
    Condition: CreateNACLs
    Properties:
      NetworkAclId: !Ref PrivateNetworkAcl
      RuleNumber: 100
      Protocol: 6
      RuleAction: allow
      CidrBlock: 10.0.0.0/16  # Adjust to your VPC CIDR
      PortRange:
        FromPort: 0
        ToPort: 65535
 
  PrivateNACLInboundEphemeral:
    Type: AWS::EC2::NetworkAclEntry
    Condition: CreateNACLs
    Properties:
      NetworkAclId: !Ref PrivateNetworkAcl
      RuleNumber: 110
      Protocol: 6
      RuleAction: allow
      CidrBlock: 0.0.0.0/0
      PortRange:
        FromPort: 1024
        ToPort: 65535
 
  # Outbound Rules - Private NACL
  PrivateNACLOutboundAll:
    Type: AWS::EC2::NetworkAclEntry
    Condition: CreateNACLs
    Properties:
      NetworkAclId: !Ref PrivateNetworkAcl
      RuleNumber: 100
      Protocol: -1
      Egress: true
      RuleAction: allow
      CidrBlock: 0.0.0.0/0
 
  PrivateSubnet1NACLAssociation:
    Type: AWS::EC2::SubnetNetworkAclAssociation
    Condition: CreateNACLs
    Properties:
      SubnetId: !Ref PrivateSubnet1
      NetworkAclId: !Ref PrivateNetworkAcl
 
  PrivateSubnet2NACLAssociation:
    Type: AWS::EC2::SubnetNetworkAclAssociation
    Condition: CreateNACLs
    Properties:
      SubnetId: !Ref PrivateSubnet2
      NetworkAclId: !Ref PrivateNetworkAcl
 
  PrivateSubnet3NACLAssociation:
    Type: AWS::EC2::SubnetNetworkAclAssociation
    Condition: CreateNACLs
    Properties:
      SubnetId: !Ref PrivateSubnet3
      NetworkAclId: !Ref PrivateNetworkAcl
 
  PrivateSubnet4NACLAssociation:
    Type: AWS::EC2::SubnetNetworkAclAssociation
    Condition: CreateNACLs
    Properties:
      SubnetId: !Ref PrivateSubnet4
      NetworkAclId: !Ref PrivateNetworkAcl
 
Outputs:
  SubnetIds:
    Description: Private Subnet IDs (comma-separated)
    Value: !If
      - CreatePrivateSubnet4
      - !Join [',', [!Ref PrivateSubnet1, !Ref PrivateSubnet2, !Ref PrivateSubnet3, !Ref PrivateSubnet4]]
      - !If
        - CreatePrivateSubnet3
        - !Join [',', [!Ref PrivateSubnet1, !Ref PrivateSubnet2, !Ref PrivateSubnet3]]
        - !If
          - CreatePrivateSubnet2
          - !Join [',', [!Ref PrivateSubnet1, !Ref PrivateSubnet2]]
          - !If
            - CreatePrivateSubnet1
            - !Ref PrivateSubnet1
            - ''
 
  RouteTableIds:
    Description: Private Route Table IDs (comma-separated)
    Value: !If
      - CreatePrivateSubnet4
      - !Join [',', [!Ref PrivateRouteTable1, !Ref PrivateRouteTable2, !Ref PrivateRouteTable3, !Ref PrivateRouteTable4]]
      - !If
        - CreatePrivateSubnet3
        - !Join [',', [!Ref PrivateRouteTable1, !Ref PrivateRouteTable2, !Ref PrivateRouteTable3]]
        - !If
          - CreatePrivateSubnet2
          - !Join [',', [!Ref PrivateRouteTable1, !Ref PrivateRouteTable2]]
          - !If
            - CreatePrivateSubnet1
            - !Ref PrivateRouteTable1
            - ''
 
  NATGatewayIds:
    Description: NAT Gateway IDs (comma-separated, if enabled)
    Condition: EnableNAT
    Value: !If
      - CreateNATGateway4
      - !Join [',', [!Ref NATGateway1, !Ref NATGateway2, !Ref NATGateway3, !Ref NATGateway4]]
      - !If
        - CreateNATGateway3
        - !Join [',', [!Ref NATGateway1, !Ref NATGateway2, !Ref NATGateway3]]
        - !If
          - CreateNATGateway2
          - !Join [',', [!Ref NATGateway1, !Ref NATGateway2]]
          - !If
            - CreateNATGateway1
            - !Ref NATGateway1
            - 'N/A'
 
  NATGatewayIps:
    Description: NAT Gateway Elastic IPs (comma-separated, if enabled)
    Condition: EnableNAT
    Value: !If
      - CreateNATGateway4
      - !Join [',', [!Ref NATGatewayEIP1, !Ref NATGatewayEIP2, !Ref NATGatewayEIP3, !Ref NATGatewayEIP4]]
      - !If
        - CreateNATGateway3
        - !Join [',', [!Ref NATGatewayEIP1, !Ref NATGatewayEIP2, !Ref NATGatewayEIP3]]
        - !If
          - CreateNATGateway2
          - !Join [',', [!Ref NATGatewayEIP1, !Ref NATGatewayEIP2]]
          - !If
            - CreateNATGateway1
            - !Ref NATGatewayEIP1
            - 'N/A'
 
  NetworkAclId:
    Description: Private Network ACL ID
    Condition: CreateNACLs
    Value: !Ref PrivateNetworkAcl
 
  SubnetCount:
    Description: Number of private subnets created
    Value: !Ref SubnetCount